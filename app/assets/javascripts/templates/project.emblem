if isEditing
  = input value=title insert-newline='doneEditing'
else
  = title
button click='edit' edit
button{action 'removeProject' project} del
= input placeholder='new task' value=newTitle action='createTask'
= partial 'tasks'
