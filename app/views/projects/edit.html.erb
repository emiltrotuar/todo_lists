<h1>Edit project</h1>
<div class="row">
	<div class="span6 offset3">
		<%= form_for(@project) do |t| %>
		<%= t.label :content %>
		<%= t.text_area :content %>
		<%= t.submit "Save changes" %>
		<% end %>
	</div>
</div>